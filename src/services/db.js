/*import { IUser } from '../interfaces';
import api from './api';

const getAllUsers = async () => {
  api.get < IUser[] > ('/employees');
  //api.get('/employees').then(response.data);
};

export const UserService = {
  getAllUsers,
};*/
