import {StyleSheet} from 'react-native';

export const stylesAreas = StyleSheet.create({
  container: {
    flex: 1,
    top: 70,
    position: 'absolute',
    width: '100%',
    alignItems: 'center',
  },
});
